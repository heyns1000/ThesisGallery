import React from 'react';
import { BrowserRouter, Routes, Route, Outlet, Link } from 'react-router-dom';

// --- Placeholder Page Components ---
// In a real project, each of these would be in its own file inside `src/pages/`
// and would contain the content from the corresponding HTML file's <main> tag.

const HomePage = () => <div><h2 className="text-4xl font-bold text-center">Welcome to the VaultKey Home Page</h2></div>;
const ProductsPage = () => <div><h2 className="text-4xl font-bold text-center">Our Products</h2><p className="text-center mt-4">Product details will be rendered here dynamically.</p></div>;
const PricingPage = () => <div><h2 className="text-4xl font-bold text-center">Pricing Plans</h2></div>;
const DashboardPage = () => <div><h2 className="text-4xl font-bold text-center">User Dashboard</h2></div>;
const AboutPage = () => <div><h2 className="text-4xl font-bold text-center">About VaultKey</h2></div>;
const FeaturesPage = () => <div><h2 className="text-4xl font-bold text-center">Application Features</h2></div>;
const LicensingPage = () => <div><h2 className="text-4xl font-bold text-center">Manage Your License</h2></div>;
const TermsPage = () => <div><h2 className="text-4xl font-bold text-center">Terms of Service</h2></div>;
const AuthPage = () => <div><h2 className="text-4xl font-bold text-center">Authentication</h2></div>;
const MetricsPage = () => <div><h2 className="text-4xl font-bold text-center">Performance Metrics</h2></div>;
const VaultkeyVisionPage = () => <div><h2 className="text-4xl font-bold text-center">Our Vision</h2></div>;

// --- Main Layout Component (The "App Shell") ---
// This is the core of the dynamic structure. It renders the header and footer
// on every page, and the <Outlet /> is where the content for each specific
// page (HomePage, ProductsPage, etc.) will be injected by the router.

const MainLayout = () => {
  return (
    <div className="bg-gray-900 text-gray-100 min-h-screen flex flex-col font-sans">
      {/* This Header is now shared across ALL pages */}
      <header className="bg-gray-800 shadow-lg py-4 px-6 md:px-10 sticky top-0 z-50">
        <div className="container mx-auto flex items-center justify-between flex-wrap">
          <div className="flex items-center">
            <span className="text-3xl mr-3">🔑</span>
            <h1 className="text-2xl md:text-3xl font-bold text-gray-400">
              <Link to="/" className="hover:text-white">VaultKey</Link>
            </h1>
          </div>
          <nav>
            {/* These links will now navigate the app WITHOUT reloading the page */}
            <ul className="flex flex-wrap justify-center md:justify-end space-x-2 md:space-x-4 text-sm md:text-base">
              <li><Link to="/" className="px-3 py-2 rounded-md hover:bg-gray-700">Home</Link></li>
              <li><Link to="/products" className="px-3 py-2 rounded-md hover:bg-gray-700">Products</Link></li>
              <li><Link to="/pricing" className="px-3 py-2 rounded-md hover:bg-gray-700">Pricing</Link></li>
              <li><Link to="/dashboard" className="px-3 py-2 rounded-md hover:bg-gray-700">Dashboard</Link></li>
              <li><Link to="/auth" className="px-3 py-2 rounded-md hover:bg-gray-700">Sign In</Link></li>
            </ul>
          </nav>
        </div>
      </header>

      {/* The main content area where page components are rendered */}
      <main className="flex-grow container mx-auto px-6 py-8 md:py-12">
        <Outlet /> {/* This is the magic part! */}
      </main>

      {/* This Footer is now shared across ALL pages */}
      <footer className="bg-gray-800 py-6 px-6 md:px-10 mt-12">
        <div className="container mx-auto text-center text-gray-400 text-sm">
          <p>&copy; 2025 VaultKey. All rights reserved.</p>
        </div>
      </footer>
    </div>
  );
};

// --- The Main Application Component ---
// This sets up the browser routing logic. It tells the app which
// page component to render inside the MainLayout based on the URL.

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route element={<MainLayout />}>
          <Route path="/" element={<HomePage />} />
          <Route path="/products" element={<ProductsPage />} />
          <Route path="/pricing" e lement={<PricingPage />} />
          <Route path="/dashboard" element={<DashboardPage />} />
          <Route path="/about" element={<AboutPage />} />
          <Route path="/features" element={<FeaturesPage />} />
          <Route path="/licensing" element={<LicensingPage />} />
          <Route path="/terms" element={<TermsPage />} />
          <Route path="/auth" element={<AuthPage />} />
          <Route path="/metrics" element={<MetricsPage />} />
          <Route path="/vision" element={<VaultkeyVisionPage />} />
        </Route>
      </Routes>
    </BrowserRouter>
  );
}

export default App;